ideaJunit {
    tasks = ['classes', 'pluginUnderTestMetadata', 'processTestResources']
}

ideaProjectComponents { components ->
    ["codeStyle", "copyright"].each {
        components.file "gradle/idea/${it}.xml"
    }
}